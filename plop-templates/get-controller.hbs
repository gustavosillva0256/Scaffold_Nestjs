import { Controller } from '@nestjs/common';
import { Crud } from '@nestjsx/crud';
import { ApiTags, ApiResponse, ApiBadRequestResponse, ApiNotFoundResponse, ApiInternalServerErrorResponse } from '@nestjs/swagger';
import { {{properCase entity}}Dto } from 'src/dtos/{{dashCase entity}}/dto/{{dashCase entity}}.dto';
import { Get{{properCase entity}}Service } from '../services/get-{{dashCase entity}}.service';

@Crud({
  model: { type: {{properCase entity}}Dto },
  query: {
    alwaysPaginate: true,
    limit: 20,
    maxLimit: 100,
    //exclude: ['fields', 'select', 'filter', 'or', 'sort', 'join', 'cache'],
  },
  routes: {
    only: ['getManyBase'],
  },
})
@ApiTags('{{properCase entity}}')
@ApiResponse({ status: 200, description: 'Consulta realizada com sucesso.', type: {{properCase entity}}Dto, isArray: true })
@ApiBadRequestResponse({ description: 'Dados inválidos ou incompletos.' })
@ApiNotFoundResponse({ description: 'Registro não encontrado.' })
@ApiInternalServerErrorResponse({ description: 'Erro interno no servidor.' })
@Controller('{{dashCase entity}}')
export class Get{{properCase entity}}Controller {
  constructor(public service: Get{{properCase entity}}Service) {}
} 